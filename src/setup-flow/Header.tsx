import { StyledHeader, StyledButton } from './setuo-flow.styles'
const Header = (): JSX.Element => {
    return (
        <StyledHeader>
            <h3>axiamatic</h3>
            <StyledButton>Exit Setup</StyledButton>
        </StyledHeader>
    );
};

export default Header;

